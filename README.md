# GIS Server — Cesium 三维校园地理信息系统

GIS Server 是一个基于 Vue 3、Vite 和 Cesium 的 WebGIS 三维可视化项目。系统围绕校园地理信息展示，提供三维场景浏览、用户入口、校友关系网络、地点变迁、历史点位、综合变化、天际线分析和飞线展示等功能。

> 本文中的界面图是根据当前路由和组件结构绘制的界面示意图，用于说明页面组成与操作路径，不是运行截图。项目包含大量本地 3D Tiles，首次克隆和加载可能需要较长时间。

## 目录

- [主要功能](#主要功能)
- [技术栈](#技术栈)
- [快速开始](#快速开始)
- [界面与操作说明](#界面与操作说明)
- [项目结构](#项目结构)
- [三维数据说明](#三维数据说明)
- [构建与部署](#构建与部署)
- [常见问题](#常见问题)

## 主要功能

- Cesium 三维地球与校园 3D Tiles 加载
- 普通用户、管理员、注册、找回密码等用户界面
- 校友关系网络可视化
- 校园地点变迁和历史点位展示
- 多类变化信息综合浏览
- 天际线分析、可视域/阴影等三维分析工具代码
- 飞线与空间关系动态展示
- 用户反馈入口

## 技术栈

| 分类 | 技术 |
| --- | --- |
| 前端框架 | Vue 3 |
| 工程工具 | Vite 2 |
| 三维 GIS | Cesium 1.92、3D Tiles |
| 路由与状态 | Vue Router 4、Vuex 4 |
| 图表与关系图 | ECharts、D3、d3-cloud |
| 空间计算 | Turf.js |
| 二维地图 | Leaflet |
| HTTP 请求 | Axios |
| 样式 | CSS、Less |

## 快速开始

### 环境要求

- Node.js 16 或更高版本（旧版 Vite 项目建议优先使用 Node.js 16/18）
- npm 8 或更高版本
- 支持 WebGL 2.0 的现代浏览器，推荐 Chrome 或 Edge
- 建议使用独立显卡和至少 8 GB 内存，以便加载大量三维瓦片

### 安装

```bash
git clone https://github.com/123poorchinwe/GIS_Server.git
cd GIS_Server
npm install
```

仓库内包含大量 `.b3dm` 三维瓦片，因此克隆时间和磁盘占用明显高于普通前端项目。

### 开发运行

```bash
npm run dev
```

打开终端输出的网址，通常为 `http://localhost:5173`。本项目使用 Hash 路由，页面地址形式为 `/#/earth`。

### 生产构建

```bash
npm run build
npm run preview
```

## 界面与操作说明

路由配置位于 `src/router/index.js`，当前共有 13 个页面。

### 1. 门户首页 `/`

系统入口页，对应 `main_page.vue`。用于介绍系统并引导用户进入三维地图、用户页面或专题功能。

```mermaid
flowchart LR
  A["系统标题与校园主题视觉"] --> B["功能入口导航"]
  B --> C["进入三维地球"]
  B --> D["用户入口"]
  B --> E["专题可视化"]
```

操作建议：从首页选择目标功能；若直接访问专题，也可在地址后使用对应 Hash 路由。

### 2. 三维地球 `/earth`

核心 Cesium 场景，对应 `cesiumMap.vue`。负责三维地球初始化、校园 3D Tiles 加载和空间交互。

```mermaid
flowchart TB
  A["三维 Cesium 场景"] --> B["校园 3D Tiles 建筑"]
  A --> C["相机缩放 / 旋转 / 倾斜"]
  A --> D["图层与分析工具"]
  D --> E["天际线 / 阴影 / 可视域"]
```

- 左键拖动：旋转或平移场景。
- 滚轮：缩放视角。
- 右键拖动：调整观察角度（具体行为受 Cesium 控制器配置影响）。
- 首次进入需要等待瓦片逐级加载，清晰度会随相机停止后逐步提高。

### 3. 管理员界面 `/superuser`

对应 `super_user.vue`，用于管理员身份入口及管理功能展示。

```mermaid
flowchart LR
  A["管理员身份信息"] --> B["管理功能区"]
  B --> C["用户/内容管理"]
  B --> D["反馈与系统入口"]
```

当前项目为前端演示时，账号校验、持久化和权限隔离是否生效取决于后端接口配置。

### 4. 普通用户界面 `/normaluser`

对应 `normal_user.vue`，提供普通用户登录或个人功能入口。

```mermaid
flowchart LR
  A["用户账号与密码"] --> B["登录操作"]
  B --> C["进入普通用户功能"]
  A --> D["注册 / 忘记密码"]
```

### 5. 注册界面 `/register`

对应 `register.vue`，用于填写新用户资料并提交注册。

```mermaid
flowchart LR
  A["填写注册资料"] --> B["校验必填项"]
  B --> C["提交注册"]
  C --> D["返回登录"]
```

提交前应确认用户名、密码和确认密码等字段符合页面提示；若未配置后端，提交可能仅表现为前端交互。

### 6. 忘记密码 `/forget`

对应 `forget_code.vue`，用于密码找回或重置流程。

```mermaid
flowchart LR
  A["输入账号信息"] --> B["身份验证"]
  B --> C["设置新密码"]
  C --> D["返回登录"]
```

实际验证码、邮件或短信发送需要配套服务端接口支持。

### 7. 用户反馈 `/feedback`

对应 `user_feedback.vue`，用于收集问题描述和使用建议。

```mermaid
flowchart LR
  A["反馈类型"] --> B["问题描述"]
  B --> C["联系方式"]
  C --> D["提交反馈"]
```

建议反馈中包含操作页面、复现步骤、浏览器版本和错误截图。

### 8. 校友关系网络 `/schoolmate`

对应 `schoolmate_network.vue`，使用校友资料与关系数据展示人物网络。

```mermaid
flowchart TB
  A["校友关系网络"] --> B["人物节点"]
  A --> C["人物关系连线"]
  B --> D["头像 / 姓名 / 资料"]
  C --> E["关系结构浏览"]
```

校友数据与头像位于 `src/assets/school_mate/`。可通过节点悬停、点击或缩放查看关系，具体交互以组件实现为准。

### 9. 地点变迁 `/location`

对应 `location_change.vue`，用于展示校园地点在不同时期的变化。

```mermaid
flowchart LR
  A["选择地点"] --> B["选择年代/阶段"]
  B --> C["历史影像或信息"]
  C --> D["前后变化对比"]
```

相关数据位于 `src/assets/location_change/location_change.json`。更新数据时应保持原 JSON 结构和图片路径一致。

### 10. 历史点位 `/history`

对应 `history_point.vue`，以地图点位方式组织历史地点或事件。

```mermaid
flowchart LR
  A["历史点位地图"] --> B["选择标记点"]
  B --> C["查看年代与简介"]
  C --> D["关联图片/资料"]
```

### 11. 综合变化 `/complex`

对应 `complex_change.vue`，综合展示多类型或多时期的校园变化信息。

```mermaid
flowchart TB
  A["综合变化专题"] --> B["时间维度"]
  A --> C["地点维度"]
  A --> D["变化类型"]
  B --> E["对比结果"]
  C --> E
  D --> E
```

### 12. 天际线分析 `/skyline`

路由关联天际线分析模块，核心实现位于 `src/utils/skyline.js` 与 `src/utils/skyline.js` 相关调用代码。

```mermaid
flowchart LR
  A["选择观察位置"] --> B["设置视角"]
  B --> C["执行天际线分析"]
  C --> D["显示轮廓/分析结果"]
```

应先等待三维模型加载完成，再执行分析；相机位置和模型精度会直接影响结果。

### 13. 飞线展示 `/flyline`

对应 `fly_line.vue`，用于显示地点之间的动态空间联系。

```mermaid
flowchart LR
  A["起点"] -->|"动态飞线"| B["终点"]
  B -->|"关系或流向"| C["空间网络"]
```

飞线适合表达迁移、联系或访问路径。若动画不显示，应检查坐标系、经纬度顺序和浏览器 WebGL 支持。

## 路由速查表

| 地址 | 页面组件 | 用途 |
| --- | --- | --- |
| `/#/` | `main_page.vue` | 门户首页 |
| `/#/earth` | `cesiumMap.vue` | Cesium 三维地图 |
| `/#/superuser` | `super_user.vue` | 管理员界面 |
| `/#/normaluser` | `normal_user.vue` | 普通用户界面 |
| `/#/register` | `register.vue` | 用户注册 |
| `/#/forget` | `forget_code.vue` | 找回密码 |
| `/#/feedback` | `user_feedback.vue` | 用户反馈 |
| `/#/schoolmate` | `schoolmate_network.vue` | 校友关系网络 |
| `/#/location` | `location_change.vue` | 地点变迁 |
| `/#/history` | `history_point.vue` | 历史点位 |
| `/#/complex` | `complex_change.vue` | 综合变化 |
| `/#/skyline` | 天际线模块 | 天际线分析 |
| `/#/flyline` | `fly_line.vue` | 飞线展示 |

## 项目结构

```text
GIS_Server/
├─ public/
│  ├─ 7/ 8/ 9/ 10/ ...        # 分级 3D Tiles 数据（.b3dm）
│  ├─ tileset*.json            # 三维瓦片入口与分块配置
│  └─ title.png                # 公共界面资源
├─ src/
│  ├─ assets/                  # 图片、样式与专题数据
│  │  ├─ location_change/      # 地点变迁数据
│  │  └─ school_mate/          # 校友头像与关系资料
│  ├─ components/              # 13 个主要业务页面与面板
│  ├─ router/index.js          # Hash 路由配置
│  ├─ utils/                   # Cesium、3D Tiles 与空间分析工具
│  ├─ App.vue
│  └─ main.js
├─ docs/examples/              # 示例 Notebook
├─ notebooks/                  # 栅格处理 Notebook
├─ package.json
└─ vite.config.js
```

## 三维数据说明

- `public/tileset.json` 及多个 `tileset_*.json` 是 3D Tiles 入口文件。
- 数字目录中的 `.b3dm` 文件是实际三维瓦片，不应随意重命名或移动。
- tileset JSON 内的相对路径必须与文件目录保持一致。
- 如果只复制源码而遗漏 `public` 瓦片目录，界面可以启动，但校园三维模型会缺失。
- 仓库体积较大时，建议考虑 Git LFS、对象存储或独立静态资源服务器。

## 构建与部署

1. 运行 `npm run build` 生成生产文件。
2. 将 `dist/` 完整部署到支持静态文件的 Web 服务器。
3. 确保服务器允许访问 `.json`、`.b3dm`、图片和字体文件。
4. 为 `.b3dm` 配置合适的 MIME 类型，例如 `application/octet-stream`。
5. 不要使用双击 HTML 文件的方式运行；Cesium 资源需要通过 HTTP/HTTPS 加载。
6. 若部署在子目录，检查 Vite `base`、Cesium 静态资源路径和 tileset 相对路径。

## 常见问题

### 三维模型不显示

- 等待瓦片下载完成并查看浏览器 Network 面板。
- 检查 `tileset*.json` 是否返回 200。
- 检查 `.b3dm` 路径、文件大小和服务器 MIME 配置。
- 查看控制台是否存在 WebGL、跨域或 Cesium 资源路径错误。

### 页面刷新后出现 404

项目采用 Hash 路由，正常地址应包含 `/#/`。部署时不要把 Hash 后面的内容交给服务器解析。

### 页面卡顿或显存占用高

- 关闭其他占用显卡的程序。
- 降低浏览器窗口分辨率。
- 限制同时加载的 tileset 数量。
- 调整 Cesium 的屏幕空间误差和缓存策略。

### 登录、注册或反馈无法真正提交

检查 Axios 请求地址和后端服务状态。仅部署前端静态文件不能自动提供用户数据库、鉴权、验证码或反馈存储服务。

### 修改专题数据后没有变化

确认 JSON 格式有效、字段名与组件一致，然后清除浏览器缓存并重新构建。

## 开发建议

- 将 Cesium Token、服务端地址等环境相关配置放入 `.env`，不要提交真实密钥。
- 为 3D Tiles 建立独立版本和备份，避免普通代码提交重复传输大量二进制文件。
- 新增页面时同时更新 `src/router/index.js` 和本文路由表。
- 发布前执行 `npm run build`，并逐页检查 13 个 Hash 路由。

## License

仓库当前未明确提供独立开源许可证。公开使用、复制或二次分发前，请先由项目所有者补充许可证文件。
